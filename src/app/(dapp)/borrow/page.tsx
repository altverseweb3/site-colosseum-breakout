"use client";

import React from "react";
import SupplyBorrowButton from "@/components/ui/BorrowSupplyButton";
import SupplyBorrowToggle from "@/components/ui/BorrowSupplyButton";

const Borrow: React.FC = () => {

    return (
        <SupplyBorrowToggle />
    );
};

export default Borrow;
